# 云盒兼容模式

> **正式功能**：由设置页「云盒兼容模式」总开关控制 C1–C4。  
> 默认**开启**（`yunheCompatDisabled` 反向字段，零值/缺键=兼容开，同 `notifyDisabled`）。  
> 口头「去掉旧版兼容」可先**关开关**；代码拆除另说（见文末）。

运行时：`compat := !cfg.Settings.YunheCompatDisabled`（`Settings.YunheCompat()`）。  
读活配置，保存即时生效，无需 MQTT 重连。中途拨开关：已进队任务的 Options 不受影响；C4 可能出现「从未发过 accepted/waiting」的空洞，可接受。

---

## 开 / 关对照

| # | 项 | 兼容开（默认） | 兼容关 |
|---|----|----------------|--------|
| **C1** | `type: 5` | 与 `type: 0` 相同（JSON 排版） | 仅 `0`/`1`；`5` → 2005 |
| **C2** | 顶层五参数 | 可省略；不传=不覆盖、不写回 | 必填；缺一 → 2001 |
| **C3** | `contents[].type=cut` | 接受；意图交 `Finish`；顶层 `cut` 优先 | 未知元素整单拒（4001） |
| **C4** | MQTT `report` | 仅 `done`/`failed` | 完整：`accepted`（含 `params`）+ `waiting` + 终态 |

**始终不变**（与开关无关）：

- `state` 全量快照 / LWT / `query:printers`
- 本地系统通知 / 告警窗
- `gateway`/`printer`、任务持久化、属性页

对照旧后端样例（兼容开须受理）：

```json
{"contents":[...{"cont":"1","type":"cut"}],"id":1615411579,"type":5,"pWidth":80,"gateway":"192.168.68.194","pCopy":1}
```

---

## 代码落点

| 项 | 路径 |
|----|------|
| 开关字段 | [`internal/model/Settings.go`](internal/model/Settings.go) `YunheCompatDisabled` / `YunheCompat()` |
| UI | [`internal/ui/SettingsView.go`](internal/ui/SettingsView.go)「服务」分组 |
| C1 / C2 / C3 | [`internal/api/Print.go`](internal/api/Print.go)、[`internal/layout/Render.go`](internal/layout/Render.go) |
| C4 | [`internal/mqtt/Client.go`](internal/mqtt/Client.go)（accepted）、[`internal/mqtt/Report.go`](internal/mqtt/Report.go)（waiting） |
| 单测 | `internal/api/LegacyCompat_test.go`、layout / mqtt 开×关矩阵 |

禁止拆成四个开关：旧后端要 C1–C3 又不认 accepted → 必须整包。

---

## 拆除代码（可选，关开关之后）

1. [ ] 删设置字段与 UI；默认走严格协议。
2. [ ] `Print.go`：删 `case 5`；`validate` 仅严格；去掉 contents cut → Options。
3. [ ] `Render.go`：删 `case "cut"`、`parseCutCont`、`compat` 参数。
4. [ ] MQTT：永久发 `accepted`+`waiting`；删兼容分支。
5. [ ] 删除本文件与过渡单测；接口文档去掉兼容表述。
