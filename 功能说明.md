# 聪明付 · 票据打印服务器 · 功能说明

面向餐厅厨房的热敏小票打印服务器（Windows 端）。云端下发打印指令，本服务在本地执行，经 USB 或网口输出到热敏小票打印机。产物为单个静态可执行文件，目标机器无需运行时依赖，兼容 Windows 7 至最新版本。

本文档为产品与操作说明。云端对接的报文格式、字段与校验规则见《接口文档.md》（或本服务内置的接口文档页，见第八章）。

---

## 一、总览

- 形态：原生 Win32 桌面程序，包含系统托盘，单个可执行文件。
- 打印机：58mm 与 80mm 热敏小票机，ESC/POS 指令，支持多品牌（佳博、飞蛾、其他；佳博与飞蛾使用同一套协议）。
- 通道：USB（经 Windows 打印驱动）与网口（TCP 9100），支持多台并发。
- 云端对接：MQTT 为唯一数据通道（见第七章概览与《接口文档.md》）。

## 二、管理控制台

左侧导航分为打印机、打印队列、系统设置三个视图，底部状态栏显示设备总数、在线数与进行中任务数。

### 2.1 打印机视图

- 多列表格：名称、来源（本地添加／云端下发）、品牌、规格、连接方式、地址或设备、状态、上次打印时间（成功出纸的绝对时间；未打过为 —）；状态列以颜色区分。
- 工具栏：新增打印机（三步向导）、刷新、测试打印、打印样票、JSON 测试（粘贴打印 JSON 直接打印，等价于云端下发）、属性、删除。
- 筛选与搜索：按全部、58、80、在线筛选；按名称、品牌、IP 搜索。

### 2.2 打印队列视图

- 任务表：编号、文档、打印机、状态、时间、详情；状态以颜色区分。多份打印以多个任务呈现，每个任务为一份。
- 操作：预览、重新打印、取消、清除已完成。预览将 JSON 排版任务按纸宽渲成卷纸图（近似效果）；原始 ESC（`type=1`）与无排版源数据的任务无法预览。
- 长时间无法送达的任务在「详情」列显示告警（红色），任务保持等待、不自动失败。
- 任务持久化：全部任务写入程序目录 `jobs.db`（SQLite）。软件重启后恢复队列；未完成任务自动继续打印；已完成／失败按设置的保留天数清理（未完成不受天数限制）。

### 2.3 系统设置

- 服务名称：用作主窗口标题与托盘提示，保存后即时生效。
- 系统通知：Windows 系统通知的启用开关，默认启用，保存后即时生效（通知内容与呈现方式见第九章）。
- 云盒兼容模式：默认开启，保存后即时生效。控制云端打印报文的字段要求与回执范围，行为详见《接口文档.md》。
- 打印历史保留天数：默认 7 天（范围 1–365）。仅自动清理「已完成／失败」记录；排队中与等待重试不受限。
- 云端 MQTT：设置页以选项卡三选一——**自建 MQTT**（Broker、端口、用户名、密码、短商户号、上报主题）、**云消息队列**（地址、端口、InstanceId、AccessKey、SecretKey、GroupId、父主题、自定义 ID、下行／上行后缀）或 **物联网平台**（「获取设备信息」：填设备源地址（web 管理端）与商户号，按本机硬件指纹查询管理端——已绑定锁定恢复、不可切换，未绑定从商户名下可认领设备选择自由打印SN；查询仅回填 ProductKey／DeviceName／DeviceSecret 与设备源地址，不产生绑定，点「保存设置」生效后才上报管理端绑定确认（幂等兼心跳）；地域／地址、下行／上行后缀→自动拼 `/{pk}/{dn}/user/{后缀}`）。启用时当前选项卡必填项须齐全，否则无法保存。连接状态与连接测试共用。
- 本地接口文档服务：启用开关、监听端口与访问地址（见第八章）。

## 三、打印通道

- USB：经已安装的 Windows 打印驱动（winspool）发送原始 ESC/POS 字节。使用前需在系统中安装该打印机的驱动。
- 网口：直接连接 `IP:9100`（标准 RAW 打印端口）。
- 每台打印机的打印与状态检测均为独立通道、并发执行，互不影响。
- 品牌差异收敛于传输层与指令层之下，新增品牌不影响上层逻辑。

## 四、打印任务与队列

打印任务进入队列后，按连接方式分为网口与 USB 两条路径。每台打印机使用一把打印串行锁，同一时刻仅建立一个连接：多份或并发任务串行打印，不同打印机之间并行。

网口打印：

- 首次派发前通过 ICMP 探测判断是否在线，离线则进入等待队列。状态探测不使用打印端口 9100。
- 实际打印结果为最终判据。打印口被占用或未就绪（连接被拒绝）时，任务进入等待队列并按 1 至 3 秒退避重试，占用释放后自动打印。
- 网络超时或不可达时，任务进入等待队列并按 5 至 30 秒退避重试，恢复连接后自动打印。
- 已连接但写入失败时，任务标记为失败，不自动重试，以避免重复出纸。
- 空闲无任务时不接触端口 9100，其他打印服务可独立使用该端口。
- 连接被拒绝持续约 20 秒后，在打印队列的详情列与日志中显示告警（端口可能未就绪或打印机故障），任务保持等待，不自动失败，不丢失请求。

USB 打印：

- 通过 Windows 打印后台（打印驱动）完成，不进行 ICMP 探测，不参与被占用或离线的重试逻辑。
- 成功提交打印后台即视为完成；打印机离线时，Windows 会在其恢复后补打。
- 提交失败（驱动、名称或配置错误）即标记为失败。

重打标题由两种方式触发：在打印队列中点击「重新打印」，或云端报文的 `reprint` 字段为 1。两种方式均在小票顶部添加醒目的「重打」标题，并遵循上述队列规则。

收尾统一执行：蜂鸣、重打标题、首部空行、打印内容、尾部走纸、按设置切纸。切纸前保证走纸充足，避免切掉底部内容。

## 五、在线状态监测

- 每台打印机分配一条独立的后台状态检测线程。网口打印机使用 ICMP 探测，约每秒一次，各自独立、互不干扰；ICMP 探测不使用打印端口 9100。
- 网口打印机：探测成功即判定在线；连续探测失败持续满 30 秒判定为离线，期间任一次探测成功立即恢复在线。离线状态的详情包含首次失败原因（含错误码，如请求超时、目标主机不可达）与已持续时长。状态列与底部「N 在线」仅在状态变化时刷新。
- USB 打印机状态通过 Windows 打印后台查询，转述打印后台的脱机标志，仅提供在线／离线，结果即时生效。打印后台未标记脱机时（如打印机断电但 USB 连接仍在），本服务显示为就绪。
- 等待重试的任务在探测恢复成功时立即被唤醒打印，不受 30 秒离线判定影响。
- 网络抖动（连续探测失败满 5 秒后恢复、未判定离线）记录一条日志，同一打印机的抖动日志至少间隔 1 分钟；每台网口打印机每 5 分钟输出一条丢包统计日志（发送数、丢失数、连败峰值、往返时延均值），统计周期内无丢包则不输出。
- 打印机由在线转为离线、或由离线恢复在线时，发送一条系统通知并同步常驻告警窗（见第九章）；程序启动后以及打印机连接属性（地址、名称等）修改后的首个判定结果不发送通知，其中首个判定即为离线的打印机仍进入常驻告警窗。
- 在线状态与打印机配置以状态快照定时上报云端：连接成功后立即一次，此后每 1 分钟一次；服务端也可下发查询消息即时拉取（消息格式见《接口文档.md》第四章与第六章）。
- 本服务不提供缺纸、开盖等物理状态的检测与上报。

## 六、每台打印机参数

- 参数项：蜂鸣、切刀（打印后是否切纸）、首部空行、尾部空行。
- 「尾部空行」为相对走纸基数的偏移量：基数按纸宽为 58mm 5 行、80mm 3 行，实际尾部走纸＝基数＋偏移（下限 0 行），属性页允许设置负偏移以减少走纸。
- 云端下发的打印以报文字段为准，并将参数覆盖持久化到打印机配置（规则见《接口文档.md》4.4）。本地的测试打印与打印样票使用属性页中的每台设置。

## 七、云端接入概览

- 通道：MQTT 3.1.1，明文；设置页三选一自建（用户名密码）、云消息队列（AK/SK 签名）或物联网平台（一机一密），断线自动重连。
- 下行：订阅解析后的下行主题接收打印消息与查询消息（自建＝短商户号；云消息队列＝`{父主题}/{自定义ID}/{下行后缀}`；物联网＝`/{ProductKey}/{DeviceName}/user/{下行后缀}`）。
- 上行：向解析后的上报主题发布打印回执（`report`）与状态快照（`state`）；全部上行携带 `merchant`（自建＝短商户号；云消息队列＝自定义 ID；物联网＝DeviceName）。
- 云消息队列父主题须在控制台预建；ClientID＝`GroupId@@@自定义ID`。物联网 ClientID＝`ProductKey.DeviceName|securemode=3,…|`；主题固定走官方 `/user/` 前缀。
- 报文格式、字段表、排版元素规范、校验与拒绝规则、限制汇总与完整示例，见《接口文档.md》或内置接口文档页。云盒兼容模式（系统设置开关，默认开启）控制报文字段要求与回执范围，规则见《接口文档.md》。
- 上行回执与状态快照均带 `merchant` 字段：自建＝短商户号，云消息队列＝自定义 ID，物联网＝DeviceName；字段名统一，各自通道内唯一。

## 八、本地接口文档服务

- 本服务内置一个仅局域网、只读的接口文档页，内容与《接口文档.md》一致。
- 该服务仅提供文档页面，不提供任何打印或数据接口；数据通信全部经由 MQTT。
- 在系统设置的「本地接口文档服务」中可启用或停用并设置监听端口（默认 8080），界面显示局域网访问地址；局域网内其他设备通过 `http://<本机局域网IP>:<端口>/` 访问。

## 九、其他

- 配置持久化：配置文件 `config.json` 与可执行文件位于同一目录，保存设置与打印机登记信息。文件损坏时自动备份为 `config.json.bad-<时间戳>`（可从备份手工恢复），并以默认配置启动，原因记录于日志。程序更新若变更配置结构，删除旧 `config.json` 后重新配置即可。
- 文件日志：日志文件 `congmingpay.log` 与可执行文件位于同一目录，记录打印机清单、状态变化、任务与错误明细以及上报失败原因，供故障排查。
- 单实例：同一台机器同时只运行一份本程序（跨用户会话同样唯一）。重复打开时弹出提示「程序已在运行，无需重复打开」并退出，不影响正在运行的实例；已运行实例的主窗口通过系统托盘图标打开。
- 系统托盘：主窗口的关闭按钮将窗口最小化到系统托盘，程序保持运行，首次最小化时发送一条系统通知提示；退出程序仅通过托盘图标右键菜单的「退出」。托盘提示为设置的服务名称，点击托盘图标打开主窗口。
- 系统通知：打印失败、打印任务长时间等待、打印机离线与恢复、云端连接断开与恢复时，经系统托盘发送 Windows 系统通知。Windows 7 呈现为托盘气泡，Windows 10 及以上由系统呈现为通知并进入通知中心。点击通知打开主窗口。可在系统设置中关闭，默认启用。本地的测试打印与打印样票失败同样通知；云端连接断开与恢复仅在启用 MQTT 期间的连接状态变化时通知，程序启动时的首个连接状态不通知。系统通知的屏幕停留时长由 Windows 系统设置控制。
- 常驻告警窗：打印失败、打印任务长时间等待、打印机离线、云端连接断开、云端打印受理失败时，屏幕右下角显示置顶告警窗，显示时不抢占输入焦点、不出现在任务栏，须手动处理或对应事件恢复方消失。每条告警包含发生时间与「已持续」时长，「已持续」每秒动态更新；打印机离线条目自持续失败的起点计时，程序启动时即离线的打印机自启动后首次探测失败计时。打印机恢复在线、云端连接恢复、任务打印成功、任务被重新打印或取消、打印机被删除时，对应条目自动消除；打印失败与云端打印受理失败条目手动清除。双击条目或点击「打开主窗口」打开主窗口；点击「全部知道了」或关闭按钮清空全部条目并收起。常驻告警窗不受系统通知开关控制：系统通知面向操作系统通知中心，常驻告警窗是本服务自身的状态面板，关闭系统通知时仍然显示。
- 打印机 ID：由时间戳与原子递增序号生成，全局唯一，并发登记不产生冲突。

## 十、技术栈与兼容性

- 语言：Go，发布使用 go1.20.14 工具链，32 位（`GOARCH=386`），单个静态可执行文件。
- 界面：Walk（原生 Win32 窗口与托盘）。
- MQTT：`github.com/eclipse/paho.mqtt.golang`（MQTT 3.1.1，兼容 go1.20 与 Windows 7）。
- 接口文档服务：Go 标准库 `net/http`，无额外依赖。
- 运行权限：普通用户，打印与网络均不需要管理员权限。
- 兼容性：Windows 7 SP1 至最新版本，支持 32 位与 64 位系统。

## 十一、规划中

- 上行协议对齐：上行消息的 `type` 与字段名为本服务与服务端的约定，待与云端最终协商确定；如需 TLS 后续再加。
- Android 端：后续独立开发。
- 更多品牌与机型的真机适配与验收。
