# 聪明付 · 票据打印服务器 · 功能说明

面向餐厅厨房的热敏小票打印服务器（Windows 端）。云端下发指令，本地服务执行，通过 USB 或网口打印。产物为单个静态可执行文件，目标机器无需运行时依赖，兼容 Windows 7 至最新版本。

云端唯一通道为 MQTT：订阅短商户号主题接收打印消息，向系统设置中配置的「上报主题」发布受理回执、打印结果、打印机列表、打印机在线离线与本服务在线状态。支持服务端单主题一对多接入（如所有客户端都发布到 `server` 主题）：每条上行消息均携带 `merchant`（短商户号）字段标识来源。不提供 HTTP 等其它数据接入方式。

---

## 一、总览

- 形态：原生 Win32 桌面程序（Walk），包含系统托盘，单个可执行文件。
- 打印机：58mm 与 80mm 热敏小票机，ESC/POS 指令，支持多品牌（佳博、飞蛾、其它；佳博与飞蛾使用同一套协议）。
- 通道：USB（经 Windows 打印驱动）与网口（TCP 9100），支持多台并发。
- 控制台：左侧导航（打印机、打印队列、系统设置），内容区，底部状态栏。
- 对接方式：MQTT 为唯一云端数据通道，订阅短商户号主题接收打印，发布回执与状态。

---

## 二、功能特性

### 1. 管理控制台

- 打印机视图：多列表格（名称、品牌、规格、连接方式、地址或设备、状态、最近活动），状态列以颜色区分。
  - 工具栏：新增打印机、刷新、测试打印、打印样票、JSON 测试（粘贴打印 JSON 直接打印，用于模拟云端下发）、属性、删除。
  - 支持按全部、58、80、在线筛选，以及按名称、品牌、IP 搜索。
- 打印队列视图：任务表（编号、文档、打印机、状态、时间、详情），状态以颜色区分，可重新打印、取消、清除已完成。多份打印（pCopy）以多个任务呈现，每个任务为一份。
- 系统设置：服务名称（用作主窗口标题与托盘提示，保存后即时生效）；云端 MQTT（启用、Broker、端口、用户名、密码、聪明付短商户号、上报主题、连接状态、连接测试）；本地接口文档服务（见第 9 节）。**启用 MQTT 时上述全部选项均为必填**（端口须为 1-65535），任一未填写或非法均无法保存。
- 新增打印机三步向导，以及属性对话框（可修改名称、品牌、蜂鸣、切刀、首尾空行、IP 与端口等）。

### 2. 打印通道（USB 与网口）

- USB：经已安装的 Windows 打印驱动（winspool）发送原始 ESC/POS 字节。使用前需在系统中安装该打印机驱动。
- 网口：直接连接 `IP:9100`（标准 RAW 打印端口）。
- 每台打印机的打印与状态检测均为独立通道、并发执行，互不影响。
- 品牌差异收敛于传输层与指令层之下，新增品牌不影响上层逻辑。

### 3. 打印内容（type=5 排版 / type=1 原始指令）

`type` 字段决定内容的处理方式：

| 取值 | contents 形式 | 处理方式 | 适用场景 |
|---|---|---|---|
| 不填、`0` 或 `5` | 排版元素数组 | 由服务端渲染为 ESC/POS，包含编码、排版与纸宽自适应 | 常见票据 |
| `1` | base64 字符串 | 解码后作为原始 ESC/POS 指令直接下发，不再排版 | 调用方提供完整 ESC/POS 指令 |

两种类型的字节均由服务端统一收尾（蜂鸣、首尾空行、按 `cut` 切纸），因此 `type=1` 的 base64 内容无需包含切纸或走纸指令。

排版元素（`type=5`）按纸张宽度自适应（58mm 为 32 字符，80mm 为 48 字符）。

**渲染为全部严格模式**：字段缺失时使用下表声明的默认值（合法）；字段存在但取值非法时**整单拒绝**——不打印任何内容，回执 `ok:false` 且 `message` 带 `contents[<下标>](<元素种类>)` 定位与原因，同时写入日志。支持的元素类型与合法值如下：

| 元素（type） | 说明与合法值 |
|---|---|
| `title` | 标题，居中加粗 |
| `text`（type 缺省时的默认） | 文本。`bold`（布尔）；`size` 缺省=不放大，存在则须为两位 0-7 数字（如 `"11"`）；`align` 缺省=左，存在则须为 left、center、right；`cont` 缺省=输出空行，存在须为字符串。未知的 `type` 值整单拒绝 |
| `div_line` / `div_star` | 分割线（`-` 或 `*`），可附带居中标题 |
| 表格 | `thead` 为对象 `{列名:"宽%"}` 或字符串数组 `["宽%"]`；每列宽须为 1-100 的整数百分比且**总和恰为 100**；`tbody` 每行单元格数须等于列数，单元格须为字符串、数字或布尔（null、对象、数组拒绝）；有 `tbody` 必须有 `thead` |
| `both_sides` | 恰为 2 段文本的数组，分别左对齐与右对齐（段数不为 2 拒绝） |
| `qrcode` | 二维码：`cont` 为字符串=单码，为恰含 2 项的字符串数组=双码;内容为空拒绝。`size` 暂不支持，忽略并记警告日志 |
| `bc128` / `code39` | 一维条码（Code128、Code39）。`size` 缺省=默认(高60/模宽2)，存在须为 `"22"` 或 `"33"`；`hri` 缺省=0(无)，存在须为 0-3；内容为空拒绝 |
| `png` | 图片：来源须为 http(s) URL 或 `data:image/...;base64,`；下载失败（含非 200 状态码）、解码失败均整单拒绝（上限 8MB、超时 10 秒） |
| `cut` | 切纸元素在渲染时被忽略，切纸由报文顶层 `cut` 字段统一控制 |

### 4. 云端下发（MQTT）

在系统设置的云端 MQTT 中填写 Broker、端口、用户名、密码、聪明付短商户号与上报主题并启用，服务将连接 broker。采用明文传输与用户名密码鉴权，断线自动重连，并提供连接测试。**启用 MQTT 时全部选项均为必填**，任一未填写无法保存。

上报主题为所有上行消息的发布主题，由配置指定（如 `server`——服务端只用一个主题接收全部客户端的上报，按消息内的 `merchant` 字段分辨来源）。若配置文件中该项为空，服务不会连接并记录日志。旧版本配置升级时自动迁移为 `<短商户号>/report` 并保存，可在系统设置中改为服务端约定的主题。

#### 4.1 下行：订阅 `<短商户号>` 接收打印

- 消息 payload 为打印 JSON，可以是单个对象或对象数组。数组中每个元素为一个独立打印请求，各自处理，互不影响。
- 收到的每一条消息都会处理并打印。`id` 字段仅用于回执与日志对应，不用于去重。
- 打印目标（`printer` 与 `gateway` 二选一）、必填字段与其它字段见第 8 节字段表。
- 自动登记：目标带有 IP 且未注册时，在打印时自动登记为新的打印机并持久化（未提供名称时以 IP 作为名称）。
- 以云端为准：下发的五个必填参数与 `printer` 身份字段（name、brand、width）覆盖并持久化至目标打印机配置。

#### 4.2 上行：发布至配置的「上报主题」

五类上行消息，全部发布至上报主题（QoS 1），且**均携带 `"merchant":"<短商户号>"` 字段标识来源**（服务端单主题一对多时据此分辨客户端）：

- 受理回执（`ack`）：每条打印请求解析与受理后立即发布 `{"type":"ack","merchant":"…","id":<原 id>,"ok":true 或 false,"jobNo":N,"message":"已提交" 或失败原因}`。`ok` 表示是否受理成功（校验、目标解析、渲染、进入队列），不代表最终打印结果。渲染类失败的 `message` 含元素定位，如 `渲染失败: contents[3](table): thead 列宽 "abc%" 非法(…)`。`id` 字段恒存在：正常回执回显原 `id`；整条消息解析失败时无法取得原 id，该回执 `id` 为 `0` 且不含 `jobNo`（建议云端使用非 0 的 `id`，以免与解析失败回执混淆）。
- 打印结果（`result`）：任务到达终态时发布 `{"type":"result","merchant":"…","id":<原 id>,"jobNo":N,"ok":true 或 false,"printer":"名称","message":"打印成功" 或失败原因,"ts":<毫秒>}`。成功（已发送至打印机/已交打印后台）为 `ok:true`；失败（已连接但写入失败、USB 交付失败等不重试场景）为 `ok:false`。处于「等待重试」的任务（离线、占用）不发布 result，待其最终打成或失败时发布；期间取消的任务不发布。手动「重新打印」再次到达终态会再次发布（同 `jobNo`）。本地操作（测试打印、打印样票）不上报。
- 打印机列表（`printerList`）：全量快照，服务器以最新一条为准（幂等）。`{"type":"printerList","merchant":"…","printers":[{"printerId":"…","printer":"名称","brand":"佳博/飞蛾/其他","width":58 或 80,"conn":"network" 或 "usb","ip":"…","port":"…"（网口）或 "usbName":"…"（USB）,"buzzer":true/false,"cut":true/false,"headLines":N,"tailLines":N,"lastPrint":"…"}],"ts":<毫秒>}`。每台条目含全部配置参数（蜂鸣、切刀、首尾空行等，即云端下发覆盖后的当前值，形成「下发 → 持久化 → 上报确认」闭环）。触发时机：①连接成功后随 `status online` 发全量基线；②云端消息导致自动登记或参数覆盖后；③本地新增、删除、属性保存后；④JSON 测试造成列表变更后。条目不含在线状态——在线离线以 `printerStatus` 消息流为准。
- 打印机在线离线（`printerStatus`）：任一打印机在线状态变化时立即发布 `{"type":"printerStatus","merchant":"…","printers":[{"printerId":"…","printer":"名称","conn":"network" 或 "usb","ip":"…","port":"…"（网口）或 "usbName":"…"（USB）,"online":true 或 false,"detail":"…"}],"ts":<毫秒>}`。`printers` 为数组，支持一条消息承载多台设备状态（当前每次包含发生变动的设备）。服务启动后每台设备的首次检测结果也会作为基线上报。
- 服务在线状态（`status`）：连接并订阅成功后发布 `{"type":"status","merchant":"…","event":"online","ts":<毫秒>}`；主动退出或异常断开（遗嘱）时发布 `{"type":"status","merchant":"…","event":"offline"}`。

发布为尽力送达：不排队、不补发；MQTT 未连接或发布失败时，原因写入日志文件 `congmingpay.log`（有据可查），消息本身不重发。断线期间产生的上行会丢失，云端可结合 `status online`、`printerList` 全量基线与受理回执自行兜底。

短商户号在输入时自动去除空格、制表符与换行，仅保留字母、数字与下划线。上行消息的 `type` 与字段为本程序约定，可与云端协商调整。

### 5. 打印任务与队列

打印任务进入队列后，按连接方式分为网口与 USB 两条独立路径。每台打印机使用一把打印串行锁，同一时刻仅建立一个连接，多份或并发任务串行打印，不同打印机之间仍并行。

网口打印：

- 首次派发前通过 ICMP 探测判断是否在线，离线则进入等待队列。状态探测不使用打印端口 9100。
- 实际打印结果为最终判据。打印口被占用或未就绪（连接被拒绝）时，任务进入等待队列并按 1 至 3 秒退避重试，占用释放后自动打印。
- 网络超时或不可达时，任务进入等待队列并按 5 至 30 秒退避重试，恢复连接后自动打印。
- 已连接但写入失败时，任务标记为失败，不自动重试，以避免重复出纸。
- 空闲无任务时不接触端口 9100，其它打印服务可独立使用该端口。
- 连接被拒绝持续约 20 秒后，在打印队列的详情列与日志中显示警告（端口可能未就绪或打印机故障），任务保持等待，不自动失败，不丢失请求。

USB 打印：

- 通过 Windows 打印后台（打印驱动）完成，不进行 ICMP 在线探测，不接触端口 9100，不参与被占用或离线的重试逻辑。
- 成功交付打印后台即视为完成。打印机离线时，Windows 会在其恢复后补打。
- 交付失败（驱动、名称或配置错误）即标记为失败，重试无益。

重打提示标题由两种方式触发：在打印队列中点击「重新打印」，或下发报文 `reprint` 为 1。两种方式均会在小票顶部添加「重打」标题，并遵循上述队列规则。`reprint` 为 0 的普通打印不显示该标题。

收尾统一执行：蜂鸣、重打标题、首部空行、打印内容、尾部走纸、按设置切纸。在切纸前保证走纸充足，避免切掉底部内容。

### 6. 在线状态监测

- 为每台打印机分配一条独立的后台状态检测线程。网口打印机使用 ICMP 探测（等效于 `ping -t` 命令），约每秒一次，各自独立，互不干扰。
- 超时判定为离线，探测成功判定为在线。状态列与底部「N 在线」实时更新，仅在状态变化时刷新。
- 在线状态发生变化（含启动后的首次检测基线）时，立即向上报主题发布 `printerStatus` 消息（见第 4.2 节）。
- ICMP 探测不使用打印端口 9100，不与打印争用通道。
- USB 打印机的状态通过 Windows 打印后台查询，用于列表显示。
- 不提供缺纸、开盖等详细状态的检测与上报（网口 ESC v 状态查询已移除；普通 USB RAW 驱动通常也不上报物理状态）。

### 7. 每台打印机参数

- 蜂鸣、切刀（是否打印后切纸）、首部空行、尾部空行。
- 云端 JSON 下发的打印以报文为准（见第 4.1 节）。本地的测试打印与打印样票使用属性页中的每台设置。

### 8. 打印报文字段

`printer` 与 `gateway` 二选一指定打印目标。`buzzer`、`cut`、`reprint`、`headLines`、`tailLines` 为必填字段，缺失任一字段或取值非法将被拒绝，回执 `ok` 为 `false`，不应用默认值。必填字段对所有内容类型均适用。

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `printer` | 字符串或对象 | 与 gateway 二选一 | 无 | 已注册打印机的名称或 ID；或对象 `{name, ip, brand, width}` |
| `gateway` | 字符串 | 与 printer 二选一 | 无 | 网口 IP，或 `"usb"` 表示首台已注册 USB 打印机 |
| `id` | 整数 | 可选 | 0 | 请求标识，仅用于回执与日志对应，不用于去重 |
| `type` | 整数 | 可选 | 5 | 内容类型，见第 3 节 |
| `pWidth` | 整数 | 可选 | 按打印机设置 | 纸张宽度（毫米），不填或 58 或 80；填其它值拒绝 |
| `pCopy` | 整数 | 可选 | 1 | 打印份数 |
| `contents` | 数组或字符串 | 可选 | 无 | 打印内容，见第 3 节 |
| `buzzer` | 整数 | 必填 | 无 | 蜂鸣，0 关闭，1 开启 |
| `cut` | 整数 | 必填 | 无 | 切刀，0 不切纸，1 切纸 |
| `reprint` | 整数 | 必填 | 无 | 重打提示标题，0 普通，1 添加「重打」标题 |
| `headLines` | 整数 | 必填 | 无 | 首部空行数，取值 0-100（0 表示默认），超出范围拒绝 |
| `tailLines` | 整数 | 必填 | 无 | 尾部空行数，取值 0-100（0 表示默认），超出范围拒绝 |

打印目标解析：

- 网口 IP（`printer.ip` 或 `gateway` 为 IP）：未注册时在打印时自动登记，端口默认 9100。自动登记必须能确定纸宽：`printer.width` 或 `pWidth` 至少一个为 58 或 80，否则拒绝（不再默认 80）。
- USB（`gateway` 为 `"usb"`，或 `printer` 指定 USB 驱动名）：选择已注册的 USB 打印机，不自动登记。
- 名称或 ID（`printer` 为字符串）：仅在已注册打印机中查找，未找到则返回错误。

### 9. 本地接口文档服务

程序内置一个仅局域网、只读的在线接口文档服务，用于查看本文档所述的接口规范。

- 该服务仅提供接口文档页面，不提供任何打印或数据接口。数据通信通过 MQTT 完成。
- 在系统设置的「本地接口文档服务」中可启用或停用，并设置监听端口（默认 8080），界面显示局域网访问地址。
- 局域网内其它设备通过 `http://<本机局域网 IP>:<端口>/` 访问。

### 10. 其它

- 配置持久化：配置文件 `config.json` 与可执行文件位于同一目录，保存设置与打印机注册表。文件损坏时自动备份为 `config.json.bad-<时间戳>`（可手工恢复打印机注册表）并以默认配置启动，原因记录于日志。
- 文件日志：日志文件 `congmingpay.log` 与可执行文件位于同一目录，记录打印机清单、状态变化、任务与错误明细，便于故障排查。
- 系统托盘：可最小化到系统托盘常驻。
- 打印机 ID：保证唯一性，并发登记不会产生冲突；启动时自动修复历史配置中的空值或重复 ID。

---

## 三、打印消息示例

单台（网口 IP，未注册时自动登记）：

```json
{
  "printer": {"name":"飞蛾1","ip":"192.168.68.194","brand":"飞蛾","width":80},
  "id": 6812801,
  "type": 5,
  "pWidth": 80,
  "pCopy": 1,
  "buzzer": 1, "cut": 1, "reprint": 0, "headLines": 0, "tailLines": 0,
  "contents": [
    {"cont":"结账单","type":"title"},
    {"cont":"【堂食】桌号：A08","type":"text","align":"center"},
    {"type":"div_line"},
    {"thead":{"商品名称":"85%","数量":"15%"},"tbody":[["猪肉大葱小笼包","1笼"]]},
    {"type":"div_line"},
    {"cont":"总计：1件","type":"text","bold":true,"align":"right"}
  ]
}
```

多台（数组，一次下发多条）：最外层为数组 `[{...}, {...}]`，每个元素结构同上，各自指定 `printer` 或 `gateway` 与内容。数组中多个元素指向同一台打印机时，该打印机按顺序依次打印。

更多可直接复制的样例见项目根目录 `测试JSON.txt`。

---

## 四、MQTT 主题

| 方向 | 主题 | 内容 |
|---|---|---|
| 下行（订阅） | `<短商户号>` | 打印 JSON，单个对象或数组 |
| 上行（发布） | 配置的「上报主题」（如 `server`） | 受理回执 `ack`、打印结果 `result`、打印机列表 `printerList`、打印机在线离线 `printerStatus`、服务在线状态 `status`（见第 4.2 节），全部携带 `merchant` 来源字段 |

- 一对多模型：服务端可只用一个主题（如 `server`）接收全部客户端上报，按消息内 `merchant`（短商户号）分辨来源；订阅主题(短商户号)各客户端独立。
- 连接：Broker 地址与端口（默认 1883），用户名与密码，明文传输，QoS 等级 1，断线自动重连，遗嘱消息为服务离线状态（发布至上报主题）。
- **启用 MQTT 时 Broker、端口、用户名、密码、短商户号、上报主题全部必填**，任一未填写或非法无法保存设置。
- 短商户号仅支持字母、数字与下划线，输入时自动去除其它字符。上报主题为完整发布主题（可含 `/`，不得含通配符 `+`、`#`，不得与短商户号即订阅主题相同——否则上报会被自己收到形成消息回环）。
- 打印消息解析失败、缺少必填字段、字段超范围、排版内容非法或目标非法时，回执 `ok` 为 `false` 并附原因（排版类含 `contents[i]` 定位），同时记录日志。
- 上行发布失败（网络异常、未连接）不排队补发，失败原因记录于日志文件。

---

## 五、技术栈与兼容性

- 语言：Go，发布使用 go1.20.14 工具链，32 位（`GOARCH=386`），单个静态可执行文件。
- 界面：Walk（原生 Win32 窗口与托盘）。
- MQTT：`github.com/eclipse/paho.mqtt.golang`（MQTT 3.1.1，不引入更高版本的 Go 或依赖，兼容 go1.20 与 Windows 7）。
- 接口文档服务：Go 标准库 `net/http`，不引入额外依赖。
- 运行权限：普通用户，打印与网络均不需要管理员权限。
- 兼容性：Windows 7 SP1 至最新版本，支持 32 位与 64 位。

---

## 六、规划中

- 上行协议对齐：上行消息（ack、result、printerList、printerStatus、status，均携带 merchant）的 `type` 与字段为本程序约定，待与云端协商确定；上报主题已可配置（支持服务端单主题一对多）；如需 TLS 后续再加。
- Android 端：后续独立开发。
- 更多品牌与机型的真机适配与验收。
