# 构建产物
/congmingpay.exe
*.exe

# 生成的 Windows 资源(build.ps1 用 rsrc 重新生成)
*.syso

# 运行时产物(与 exe 同目录,机器相关;config.json.* 覆盖损坏备份与手工备份,含 MQTT 凭据不入库)
/config.json
/config.json.*
*.log
*~

# 临时/测试产物
*.smokebak
测试JSON.old
/测试JSON.old/

# 厂商 SDK(仅参考、体积大)与设计稿存档
/SDK/
/_design/
/50-80.zip

# web 管理系统运行数据(web.db、log,不入库)
web/backend/data/

# 依赖(MCP 工具 edge-devtools-mcp 等)
node_modules/

# 设备证书导出(含 DeviceSecret,不入库)
/设备证书.csv
web/certs.csv

# 临时/工具目录(不入库)
.qwen/
.tmp/
.tmpedge/
