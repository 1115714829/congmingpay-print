# 构建产物
/congmingpay.exe
*.exe

# 生成的 Windows 资源(build.ps1 用 rsrc 重新生成)
*.syso

# swaggo 生成物(swagger.json 内嵌需保留;docs.go/yaml 由 build.ps1 删除)
internal/api/docs/docs.go
internal/api/docs/swagger.yaml

# 运行时产物(与 exe 同目录,机器相关)
/config.json
*.log

# 临时/测试产物
*.smokebak
测试JSON.old
/测试JSON.old/

# 厂商 SDK(仅参考、体积大)与设计稿存档
/SDK/
/_design/
/50-80.zip
