# 聪明付票据打印服务器 接口文档

## 一、文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 聪明付票据打印服务器 接口文档 |
| 文档版本 | v1.4 |
| 发布日期 | 2026-07-08 |
| 适用范围 | 云端／服务端与本服务（Windows 打印客户端）之间的 MQTT 接口对接 |
| 维护约定 | 本文档与程序内置接口文档页（`internal/docserver/apidoc.html`，局域网 `http://<主机IP>:<端口>/` 访问）内容保持同步 |

---

## 二、术语与约定

### 2.1 术语表

| 术语 | 说明 |
|---|---|
| 本服务 | 聪明付票据打印服务器（Windows 端程序），MQTT 客户端角色 |
| 短商户号（merchant） | 聪明付分配的商户标识，仅含字母、数字与下划线。用作下行订阅主题，并作为全部上行消息的来源标识字段 |
| 上报主题 | 本服务发布全部上行消息的 MQTT 主题，由本服务设置页配置（例如 `server`） |
| 打印机 | 本服务中登记的一台热敏小票打印机（网口或 USB） |
| 任务 | 一次打印请求经受理后生成的打印作业，以任务号（`jobNo`）标识 |
| 打印回执（report） | 上行消息之一：打印请求的受理与执行结果，`event` 字段区分阶段（受理／卡单／成功／失败） |
| 状态快照（state） | 上行消息之一：服务在线状态与全部打印机（配置参数＋在线状态）的全量快照 |
| 错误码（code） | 全局分段数字码，0＝成功，非 0 时首位数字表示阶段，见第七章 |
| 整单拒绝 | 打印请求中任一内容非法时，整条请求不打印，打印回执 `event` 为 `failed` 并携带错误码 |
| 纸宽 | 热敏纸宽度，取值 58 或 80（毫米）。58mm 每行 32 字符，80mm 每行 48 字符（中文按 2 字符计） |

### 2.2 数据约定

- 报文均为 UTF-8 编码的 JSON。
- 字段表中「必填」列：必填＝缺失即拒绝；可选＝缺失时按「默认」列取值。
- **严格校验总则**：字段缺失时按本文档声明的默认值处理（合法）；字段存在但取值非法时**整单拒绝**，不应用默认值、无降级。显式 `null` 视作字段缺失。仅校验被该元素实际消费的字段。

---

## 三、通信通道

### 3.1 连接参数

| 参数 | 说明 |
|---|---|
| 协议 | MQTT 3.1.1，明文 TCP |
| Broker 地址／端口 | 由本服务设置页配置，端口默认 `1883` |
| 鉴权 | 用户名／密码 |
| ClientID | 填写短商户号 |
| KeepAlive | 60 秒 |
| 会话 | CleanSession；断线自动重连（重连间隔最大 30 秒），重连后自动恢复订阅 |
| QoS | 订阅与发布均为 QoS 1 |
| 遗嘱（LWT） | 主题＝上报主题，内容＝简版状态快照（服务离线，见 6.3） |

### 3.2 主题

| 方向 | 主题 | 内容 |
|---|---|---|
| 下行（本服务订阅） | `<短商户号>` | 打印消息：单个 JSON 对象或对象数组（见第四章） |
| 上行（本服务发布） | 上报主题 | 两类上行消息：打印回执 `report` 与状态快照 `state`（见第六章），全部携带 `merchant` 字段 |

单主题一对多模型：服务端可仅使用一个上报主题（例如 `server`）接收全部客户端的上行消息，按消息内 `merchant` 字段区分来源；各客户端的下行订阅主题（短商户号）相互独立。

上报主题约束：为完整发布主题，可包含 `/`；不得包含通配符 `+`、`#`；不得等于订阅主题（短商户号），否则上行消息将被本服务自身接收并形成消息循环。

### 3.3 投递语义

- 上行发布为尽力送达：不排队、不补发。MQTT 未连接或发布失败时，消息丢弃，原因记录于本服务日志文件 `congmingpay.log`。
- 断线期间产生的上行消息不可恢复。状态快照（state）为定时全量上报（见 6.3），单条丢失由后续周期自动补齐；服务端可按「超过约 2.5 分钟未收到 `state`」判定客户端异常，并结合打印回执（report）自行兜底。

---

## 四、下行接口

下行报文按顶层字段分流：顶层 `query` 为非空字符串的单个对象为查询消息（见 4.6）；其余为打印消息（单个打印请求对象，或打印请求对象的数组）。

### 4.1 打印消息报文结构

消息 payload 为一个打印请求对象，或打印请求对象的数组。数组中每个元素为一条独立请求，各自受理、各自回执，互不影响；本服务不做去重，收到的每条请求均会处理。

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `printer` | 字符串 或 对象 | 与 `gateway` 二选一 | — | 打印目标。字符串＝已登记打印机的名称或 ID（网口与 USB 通用）；对象见 4.1.1 |
| `gateway` | 字符串 | 与 `printer` 二选一 | — | 网口地址 `IP` 或 `IP:端口`（端口缺省 9100）。仅承载网口地址；USB 打印机以 `printer` 指定名称或 ID |
| `id` | 整数 | 可选 | 0 | 请求标识，取值 0–4294967295。仅用于回执与日志对应，不用于去重。建议使用非 0 值且每单唯一 |
| `type` | 整数 | 可选 | 0 | 内容类型，见 4.5 |
| `pWidth` | 整数 | 可选 | 按打印机登记纸宽 | 渲染纸宽（毫米），取值 58 或 80；填其他值拒绝 |
| `pCopy` | 整数 | 可选 | 1 | 打印份数，小于等于 0 按 1 处理；每份生成一个任务，同一打印机按序打印 |
| `contents` | 数组 或 字符串 | 可选 | — | 打印内容：`type=0` 为排版元素数组（第五章）；`type=1` 为 base64 字符串 |
| `buzzer` | 整数 | 必填 | — | 蜂鸣：`0` 关闭，`1` 开启 |
| `cut` | 整数 | 必填 | — | 切刀：`0` 不切纸，`1` 切纸 |
| `reprint` | 整数 | 必填 | — | 重打标题：`0` 普通，`1` 在小票顶部添加醒目「重打」标题 |
| `headLines` | 整数 | 必填 | — | 首部空行数（绝对值），取值 0–100；`0` 表示不加首部空行 |
| `tailLines` | 整数 | 必填 | — | 尾部走纸偏移，取值 0–100。实际尾部走纸行数＝基数＋偏移（基数：58mm 为 5 行，80mm 为 3 行）；`0` 表示按基数走纸 |

注：`id` 超出 uint32 范围（负数或大于 4294967295）会导致整条 payload 解析失败；数组 payload 中任一元素解析失败，则整个数组解析失败（见 4.2）。

#### 4.1.1 `printer` 对象

| 字段 | 类型 | 说明 |
|---|---|---|
| `name` | 字符串 | 打印机名称。用于按名称选择已登记打印机；自动登记时作为新打印机名称，缺省时以 IP 作为名称 |
| `id` | 字符串 | 打印机 ID。仅用于选择已登记打印机 |
| `ip` | 字符串 | 网口地址。优先级高于 `gateway` |
| `port` | 字符串 | 网口端口，缺省 `9100` |
| `brand` | 字符串 | 品牌：`佳博`、`飞蛾`、`其他`。自动登记与身份覆盖时使用，缺省为 `其他` |
| `width` | 整数 | 纸宽 58 或 80。自动登记与身份覆盖时使用 |

### 4.2 校验与拒绝

- `buzzer`、`cut`、`reprint`、`headLines`、`tailLines` 五个字段对所有内容类型均为必填。缺失任一字段、`buzzer`／`cut`／`reprint` 取值非 0 或 1、`headLines`／`tailLines` 超出 0–100、`pWidth` 填写了非 58／80 的值，该条请求即被拒绝，打印回执 `event=failed` 并携带错误码与原因（第七章）。
- `contents` 内容非法时整单拒绝，规则见第五章。
- 整条 payload 无法解析为 JSON（或字段类型不符，如 `id` 越界）时，本服务发布一条 `event=failed`、`code=1001` 的打印回执，其 `id` 为 0 且不含 `jobNo`，`message` 以「解析失败」开头。

### 4.3 打印目标解析

按以下顺序确定目标（`printer.ip` 优先于 `gateway`）：

1. **网口地址**（`printer.ip`，或 `gateway` 为 `IP`／`IP:端口`）：
   - 该 IP 已登记：直接使用该打印机。
   - 未登记：自动登记为新打印机。自动登记要求纸宽可确定——`printer.width` 或 `pWidth` 至少一个为 58 或 80，否则拒绝；名称缺省取 IP，品牌缺省 `其他`，端口缺省 `9100`。
2. **名称／ID 选择器**（`printer` 为字符串，或对象仅含 `name`／`id`）：在已登记打印机中按 ID 或名称精确匹配，未找到则拒绝。名称与 ID 与状态快照（state，见 6.3）上报的 `printer`／`printerId` 对应。
3. `printer` 与 `gateway` 均未提供：拒绝。

USB 打印机仅可经名称／ID 选择器指定；`gateway` 填 `"usb"` 的请求拒绝（`code=3003`）。USB 目标不自动登记。

### 4.4 参数持久化（以云端为准）

每条受理成功的打印请求，会将以下内容覆盖并持久化到目标打印机配置：

- 参数四项：`buzzer`、`cut`、`headLines`、`tailLines`；
- 身份字段：`printer.name`、`printer.brand`、`printer.width`（仅在提供且合法时覆盖，缺省不改动原值）。

`reprint` 为每单行为，仅对本单生效，不持久化。持久化后的参数以状态快照（state，见 6.3）回传，形成下发、持久化、上报确认的闭环。打印机属性页中的参数仅用于本地测试打印。

### 4.5 内容类型（type）

| 取值 | `contents` 形式 | 处理方式 |
|---|---|---|
| 缺省 或 `0` | 排版元素数组 | 由本服务渲染为 ESC/POS，按纸宽自适应排版（第五章） |
| `1` | base64 字符串 | 解码后作为原始 ESC/POS 指令直接下发，不进行排版 |

其他取值拒绝。两种类型均由本服务统一收尾：蜂鸣、重打标题、首部空行、尾部走纸与切纸由报文顶层字段控制，`contents` 内无需（也不应）包含走纸或切纸指令。

### 4.6 查询消息

服务端向短商户号主题下发查询消息，按需拉取本服务的最新状态：

```json
{"query":"printers"}
```

- 查询消息为单个对象，顶层 `query` 为非空字符串；不支持放入数组。
- `query` 为 `"printers"`：本服务立即向上报主题发布一条全量状态快照（`state`，结构与定时上报一致，见 6.3）。
- `query` 为其他取值：本服务发布一条打印回执 `event=failed`、`code=1002`、`id=0`。

---

## 五、排版元素规范（type=0）

### 5.1 总则

- `contents` 为排版元素数组，按数组顺序渲染；每个元素为一个 JSON 对象。
- **整单拒绝**：任一元素含非法取值时，整条请求不打印，打印回执 `event=failed`、`code=4001`，`message` 格式为 `渲染失败: contents[<下标>](<元素种类>): <原因>`，下标为 0 基。
- 字段缺失（含显式 `null`）按各元素声明的默认值处理；仅校验被该元素消费的字段，未被消费的字段忽略。
- 元素种类由 `type` 字段决定；含 `thead` 的元素为表格（无需 `type`）；含 `both_sides` 的元素为左右两列（无需 `type`）。未知的 `type` 取值整单拒绝。

### 5.2 text（文本；`type` 缺省时的默认元素）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `cont` | 字符串 | 可选 | 空行 | 文本内容；缺省输出一个空行 |
| `bold` | 布尔 | 可选 | `false` | 加粗 |
| `size` | 字符串 | 可选 | 不放大 | 两位数字 `"WH"`，W＝宽放大档、H＝高放大档，各 0–7（`"11"` 表示宽高各放大一档，即二倍字）。非两位或超出 0–7 拒绝 |
| `align` | 字符串 | 可选 | `left` | `left`、`center`、`right`；其他取值拒绝 |

示例：`{"type":"text","cont":"桌号：A08","size":"11","bold":true,"align":"center"}`

### 5.3 title（标题）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `cont` | 字符串 | 可选 | 空行 | 标题内容 |

固定样式：居中、加粗、二倍字；不消费 `size`、`align`、`bold`。

示例：`{"type":"title","cont":"结账单"}`

### 5.4 div_line／div_star（分割线）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `cont` | 字符串 | 可选 | 整行填充 | 居中标题；提供时分割线形如 `---- 标题 ----` |

`div_line` 以 `-` 填充，`div_star` 以 `*` 填充，宽度为整行。

示例：`{"type":"div_line"}`、`{"type":"div_star","cont":"二维码"}`

### 5.5 表格（含 `thead` 的元素）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `thead` | 对象 或 字符串数组 | 必填 | — | 对象 `{列名:"宽%"}`（保持书写顺序，渲染表头行）或字符串数组 `["宽%"]`（不渲染表头行）。每列宽度须为 1–100 的整数百分比（容忍 `%` 后缀与空白），**总和必须恰为 100** |
| `tbody` | 二维数组 | 可选 | 空 | 数据行。每行单元格数必须等于列数；单元格仅接受字符串、数字、布尔（`null`、对象、数组拒绝） |
| `size` | 字符串 | 可选 | 不放大 | 两位数字，规则同 text；表格取第一位作为宽高共同放大档 |
| `line_div` | 整数 | 可选 | 0 | 为 `1` 时每行之后输出一条 `-` 分割线 |
| `line_space` | 整数 | 可选 | 0 | 每行之后额外走纸的行数 |

约束：有 `tbody` 而无 `thead` 拒绝。单元格内容超出列宽时按显示宽度自动换行（中文按 2 计）。

示例：

```json
{"thead":{"商品名称":"70%","数量":"15%","金额":"15%"},
 "tbody":[["青椒肉丝（标准）","1份",0.01],["规格测试","1份",0.01]],"size":"00"}
```

### 5.6 both_sides（左右两列）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `both_sides` | 字符串数组 | 必填 | — | 恰为 2 个元素：左侧文本与右侧文本，分别左对齐与右对齐；元素个数不为 2 拒绝 |
| `size` | 字符串 | 可选 | 不放大 | 两位数字，规则同 text |

示例：`{"both_sides":["实付金额","0.02"],"size":"11"}`

### 5.7 qrcode（二维码）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `cont` | 字符串 或 字符串数组 | 必填 | — | 字符串＝单码；恰含 2 项的字符串数组＝并排双码。内容为空拒绝；数组元素个数不为 2 拒绝 |
| `align` | 字符串 | 可选 | `left` | 对齐，规则同 text |
| `size` | 字符串 | 可选 | — | 不消费该字段：忽略并记录警告日志，按默认模块大小渲染 |

约束：单码内容上限 2953 字节（QR Model 2 容量）；双码内容超出容量时因生成失败而整单拒绝。

示例：`{"type":"qrcode","cont":"https://example.com/pay/123","align":"center"}`

### 5.8 bc128／code39（一维条码）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `cont` | 字符串 | 必填 | — | 条码内容，非空且不超过 250 字节 |
| `size` | 字符串 | 可选 | 高 60 点／模块宽 2 | `"22"`＝高 90／宽 3，`"33"`＝高 120／宽 4；其他取值拒绝 |
| `hri` | 整数 | 可选 | 0 | 可读字符位置：`0` 无，`1` 上方，`2` 下方，`3` 上下；其他取值拒绝 |
| `align` | 字符串 | 可选 | `left` | 对齐，规则同 text |

`bc128` 为 Code128（码集 B），`code39` 为 Code39；字符集兼容性由打印机固件决定。

示例：`{"type":"bc128","cont":"10132607070942533965136","align":"center","size":"22","hri":2}`

### 5.9 png（图片）

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `cont` | 字符串 | 必填 | — | 图片来源：`http(s)` URL，或 `data:image/...;base64,<数据>` |
| `align` | 字符串 | 可选 | `left` | 对齐，规则同 text |

- 图片按内容识别，支持 PNG、JPEG、GIF 编码；渲染为单色位图（含抖动）。
- URL 来源：下载超时 10 秒、大小上限 8MB，HTTP 状态码非 200 拒绝。
- 下载失败、来源格式非法、解码失败均整单拒绝。

示例：`{"type":"png","cont":"data:image/png;base64,iVBOR...","align":"center"}`

---

## 六、上行接口

### 6.1 通用约定

上行消息共两类：**打印回执（`report`）**与**状态快照（`state`）**。

- 全部上行消息发布至上报主题，QoS 1。
- 全部上行消息携带 `"merchant":"<短商户号>"` 字段，标识消息来源客户端。
- 全部上行消息携带 `"type"` 字段标识消息类型；`ts` 为毫秒级 Unix 时间戳。

### 6.2 report（打印回执）

打印请求的受理与执行结果，统一结构，`event` 字段区分阶段：

| event | 时机 | code | 携带内容 |
|---|---|---|---|
| `accepted` | 请求受理成功、进入打印队列 | 0 | `printer`＋`params`（本单完整上下文） |
| `failed` | 受理失败（解析／校验／目标／渲染） | 1xxx–4xxx | 目标已解析时携带 `printer` |
| `waiting` | 任务持续被拒超 20 秒（每单至多一次） | 5101 | `printer` |
| `done` | 打印成功（数据已发送至打印机，或已提交 Windows 打印队列） | 0 | `printer` |
| `failed` | 打印执行失败（写入失败、USB 提交失败等不重试场景） | 5001／5002 | `printer` |

每单消息序：`accepted →（waiting）→ done／failed`；受理失败时仅一条 `failed`。

- 处于「等待重试」的任务（打印机离线、打印口被占用）在终态前仅可能发布一条 `waiting`；等待期间被取消的任务不再发布。
- 在本服务界面执行「重新打印」的任务再次到达终态时重复发布（`jobNo` 相同）。
- 本地操作（测试打印、打印样票）不上报。

```json
{"type":"report","merchant":"M100001","event":"accepted","id":6812801,"jobNo":1001,
 "code":0,"message":"已提交",
 "printer":{"printerId":"p1720000000-1","printer":"飞蛾1","brand":"飞蛾","width":80,
            "conn":"network","ip":"192.168.68.194","port":"9100"},
 "params":{"buzzer":1,"cut":1,"reprint":0,"headLines":0,"tailLines":0,"pWidth":80,"pCopy":1,"contentType":0},
 "ts":1720000000000}
```

```json
{"type":"report","merchant":"M100001","event":"done","id":6812801,"jobNo":1001,
 "code":0,"message":"打印成功",
 "printer":{"printerId":"p1720000000-1","printer":"飞蛾1","brand":"飞蛾","width":80,
            "conn":"network","ip":"192.168.68.194","port":"9100"},
 "ts":1720000000000}
```

```json
{"type":"report","merchant":"M100001","event":"failed","id":6812802,
 "code":4001,"message":"渲染失败: contents[3](table): thead 列宽 \"abc%\" 非法(需 1-100 的整数百分比)","ts":1720000000000}
```

| 字段 | 说明 |
|---|---|
| `event` | 阶段，见上表 |
| `id` | 回显请求的 `id`，字段恒存在。整条 payload 解析失败时无法取得原 `id`，回执 `id` 为 0 且不含 `jobNo`。建议云端使用非 0 的 `id` 以便区分 |
| `jobNo` | 任务号；同一 `jobNo` 的各阶段回执据此关联 |
| `code` | 全局错误码，0＝成功，非 0 见第七章 |
| `message` | 成功为 `"已提交"`／`"打印成功"`；失败与等待为原因文本（格式见第七章） |
| `printer` | 打印机身份：`printerId`／`printer`（名称）／`brand`／`width`／`conn`／`ip`＋`port`（网口）或 `usbName`（USB）。目标未解析时省略 |
| `params` | 本单生效参数（仅 `accepted` 携带）：`buzzer`／`cut`／`reprint`／`headLines`／`tailLines`／`pWidth`（实际渲染纸宽）／`pCopy`（实际份数）／`contentType` |

### 6.3 state（状态快照）

服务在线状态与全部打印机（配置参数＋在线状态）的**全量快照**；服务端以最新一条为准（幂等），无需拼装增量。

发布时机（定时上报）：①连接成功后立即发布一次；②此后每 1 分钟发布一次；③收到查询消息 `{"query":"printers"}` 时发布一次（见 4.6）。打印机状态与配置的变化不即时上报，最坏滞后 1 分钟；需要即时对账时服务端下发查询消息。

```json
{"type":"state","merchant":"M100001","online":true,
 "printers":[
  {"printerId":"p1720000000-1","printer":"飞蛾1","brand":"飞蛾","width":80,"conn":"network",
   "ip":"192.168.68.194","port":"9100","online":true,"detail":"就绪(ping 3ms)",
   "buzzer":true,"cut":true,"headLines":0,"tailLines":0,"lastPrint":"云端下发"}
 ],"ts":1720000000000}
```

离线为简版（本服务正常退出、MQTT 停用或连接配置变更时主动发布；异常断开时由 broker 按遗嘱发布）：

```json
{"type":"state","merchant":"M100001","online":false}
```

| 字段 | 说明 |
|---|---|
| `online` | 本服务是否在线。`false` 时为简版，不含 `printers` 与 `ts` |
| `printers[].printerId`／`printer` | 打印机 ID 与名称 |
| `printers[].brand`／`width` | 品牌（`佳博`／`飞蛾`／`其他`）与纸宽（58／80） |
| `printers[].conn` | `"network"`（网口）或 `"usb"` |
| `printers[].ip`／`port` | 网口地址与端口（USB 时省略） |
| `printers[].usbName` | USB 驱动名（网口时省略） |
| `printers[].online`／`detail` | 该打印机是否在线与可读描述（检测方式与耗时等） |
| `printers[].buzzer`／`cut`／`headLines`／`tailLines` | 每台参数当前值（云端下发覆盖后的持久化结果）。注：`tailLines` 为尾部走纸偏移量，本地属性页允许设置负值 |
| `printers[].lastPrint` | 最近打印描述（展示用途，可省略） |

---

## 七、错误码

打印回执（report）的 `code` 字段为全局错误码：`0`＝成功；非 0 时首位数字表示阶段（1 报文解析／2 字段校验／3 打印目标／4 内容渲染／5 打印执行）。`message` 为对应的中文原因文本，排版类错误带 `contents[<下标>](<元素种类>)` 定位。

| code | 含义 | message 示例 |
|---|---|---|
| 1001 | 报文解析失败（`id` 回 0） | `解析失败: <JSON 错误>` |
| 1002 | 不支持的 `query` 取值（`id` 回 0） | `不支持的 query: foo(支持 "printers")` |
| 2001 | 缺少必填字段 | `缺少必填字段: buzzer、cut(buzzer/cut/reprint/headLines/tailLines 均必传)` |
| 2002 | 开关取值非法 | `buzzer 只能为 0 或 1` |
| 2003 | 空行范围非法 | `headLines 需在 0-100 之间` |
| 2004 | `pWidth` 非法 | `pWidth 需为 58 或 80(或不填)` |
| 2005 | 内容类型不支持 | `不支持的 type: 3(仅 0=JSON 排版 / 1=ESC base64)` |
| 3001 | 未指定打印目标 | `未指定打印目标(printer 或 gateway 至少填一个)` |
| 3002 | 打印机未找到 | `未找到打印机: <名称或ID>` |
| 3003 | USB 目标非法 | `USB 打印以 printer 指定打印机名称(gateway 仅支持网口 IP)` |
| 3004 | 无法确定纸宽 | `无法确定纸宽: 自动登记新打印机需 printer.width 或 pWidth 为 58/80(收到 printer.width=0、pWidth=0)` |
| 4001 | 排版内容非法 | `渲染失败: contents[3](table): thead 列宽 "abc%" 非法(需 1-100 的整数百分比)` |
| 4002 | 排版数组非法（`contents` 整体非元素数组） | `渲染失败: <JSON 错误>` |
| 4003 | 文本编码失败（GB18030 无法编码的字符） | `渲染失败: <编码错误>` |
| 4004 | ESC 指令解码失败（`type=1`） | `渲染失败: <base64/JSON 错误>` |
| 5001 | 网口写入失败（终态） | 底层网络错误原文 |
| 5002 | USB 提交失败（终态） | `打开打印机 "..." 失败: ...` 等 |
| 5101 | 长期等待告警（event=`waiting`，非终态） | `长期等待·疑似端口未就绪/故障(请检查 IP/端口/打印机): ...` |
| 9001 | 未知错误（兜底） | — |

---

## 八、限制与约束汇总

| 项目 | 约束 |
|---|---|
| `id` | 0–4294967295（uint32） |
| `pWidth` | 缺省，或 58／80 |
| `headLines` | 0–100（绝对空行数） |
| `tailLines` | 0–100（相对基数偏移；基数 58mm＝5 行、80mm＝3 行） |
| `size`（text／表格／both_sides） | 两位数字，每位 0–7 |
| 表格列宽 | 每列 1–100 的整数百分比，总和恰为 100 |
| 表格行 | 每行单元格数＝列数；单元格仅字符串／数字／布尔 |
| `both_sides` | 恰 2 个字符串元素 |
| 条码内容 | 非空，≤ 250 字节 |
| 单码二维码内容 | 非空，≤ 2953 字节 |
| 图片（URL 来源） | ≤ 8MB，下载超时 10 秒，HTTP 200 |
| 上报主题 | 可含 `/`；禁 `+`、`#`；不得等于短商户号 |
| 每行字符数 | 58mm＝32，80mm＝48（中文按 2 计） |

---

## 九、完整示例

### 9.1 单台（printer 对象，未登记则自动登记）

```json
{
  "printer": {"name":"飞蛾1","ip":"192.168.68.194","brand":"飞蛾","width":80},
  "id": 6812801,
  "type": 0,
  "pWidth": 80,
  "pCopy": 1,
  "buzzer": 1, "cut": 1, "reprint": 0, "headLines": 0, "tailLines": 0,
  "contents": [
    {"cont":"结账单","type":"title"},
    {"cont":"【堂食】桌号：A08","type":"text","align":"center"},
    {"type":"div_line"},
    {"thead":{"商品名称":"85%","数量":"15%"},"tbody":[["猪肉大葱小笼包","1笼"]]},
    {"type":"div_line"},
    {"both_sides":["实付金额","0.02"]},
    {"cont":"总计：1件","type":"text","bold":true,"align":"right"}
  ]
}
```

### 9.2 多台（数组，一次下发多条）

最外层为数组 `[ {…}, {…} ]`，每个元素结构同 9.1，各自指定 `printer` 或 `gateway` 与内容。多个元素指向同一台打印机时，该打印机按顺序依次打印。

### 9.3 gateway 指定网口地址

```json
{
  "gateway": "192.168.68.194",
  "id": 6812858,
  "type": 0,
  "pWidth": 58,
  "pCopy": 1,
  "buzzer": 1, "cut": 1, "reprint": 0, "headLines": 0, "tailLines": 0,
  "contents": [
    {"cont":"取餐号 A012","type":"title"},
    {"thead":{"商品名称":"75%","数量":"25%"},"tbody":[["小笼包","1笼"]]}
  ]
}
```

### 9.4 名称指定 USB 打印机

```json
{
  "printer": "打印1",
  "id": 1615411580,
  "type": 0,
  "pWidth": 80,
  "pCopy": 1,
  "buzzer": 1, "cut": 1, "reprint": 0, "headLines": 0, "tailLines": 0,
  "contents": [ {"cont":"USB 打印","type":"title"} ]
}
```

### 9.5 type=1（原始 ESC/POS）

```json
{
  "gateway": "192.168.68.194",
  "id": 1615411581,
  "type": 1,
  "pWidth": 80,
  "pCopy": 1,
  "buzzer": 0, "cut": 1, "reprint": 0, "headLines": 0, "tailLines": 0,
  "contents": "G0BIZWxsbyBFU0MvUE9TCg=="
}
```

`contents` 为完整 ESC/POS 指令的 base64 编码；走纸与切纸仍由顶层字段统一收尾，指令内无需包含。

---

## 十、补充约定

- 上报主题无默认值，须在本服务设置页配置；未配置时本服务不连接 MQTT，原因记录于日志。
- 上行消息的 `type` 与字段名为本服务与服务端的约定，如需调整以双方协商结果为准。

---

## 十一、修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-07-08 | 初版：通信通道、下行打印消息、排版元素规范、五类上行消息、错误与限制汇总 |
| v1.1 | 2026-07-08 | ClientID 改为短商户号；内容类型 `type` 取值改为 `0`（JSON 排版，默认）与 `1`（原始 ESC/POS）；移除 `cut` 排版元素 |
| v1.2 | 2026-07-08 | 上行消息合并为 `report`（打印回执）与 `state`（状态快照）两类；新增全局错误码（`code` 字段与第七章码表）；新增 `waiting` 卡单事件；`accepted` 回执携带打印机身份与本单参数 |
| v1.3 | 2026-07-08 | USB 打印目标以打印机名称或 ID 指定（与状态快照上报对应）；`gateway` 仅承载网口地址；移除 `printer.usbName` 寻址与 `gateway:"usb"` 首台匹配；错误码 3003 改为「USB 目标非法」 |
| v1.4 | 2026-07-08 | `state` 状态快照改为定时上报（连接成功立即一次、此后每 1 分钟一次）；新增下行查询消息 `{"query":"printers"}` 按需拉取全量 `state`；移除状态变化即时上报；新增错误码 1002 |
